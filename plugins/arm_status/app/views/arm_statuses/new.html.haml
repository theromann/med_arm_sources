%h2
  = link_to(l(@arm_status.option_name), arm_statuses_path)
  »
  =l("label.contact_status_new")

= labelled_form_for :arm_status, @arm_status, :url => arm_statuses_path do |f|
  = f.hidden_field :type
  = render :partial => 'form', :locals => {:f => f}
  = submit_tag l(:button_create)
